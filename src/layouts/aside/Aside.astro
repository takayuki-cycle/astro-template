---
import Category from '@/components/category/Category.astro'
import type { AsideProps } from '@/layouts/aside/types'

const { pathName, category } = Astro.props.aside as AsideProps
---

<aside>
  {pathName && <Category pathName={pathName} category={category} />}
</aside>
