---
import { Category } from '@/components'
import type { AsideProps } from '@/layouts/aside/types'

const { pathName, category }: AsideProps = Astro.props.aside
---

<aside>
  {pathName && <Category pathName={pathName} category={category} />}
</aside>
